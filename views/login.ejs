<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login | PlanIt</title>
  <link rel="stylesheet" href="/main.css" />
  <link rel="stylesheet" href="/user.css" />
</head>
<body>
  
  <header class="top-nav">
    <div class="nav-buttons">
      <% if (user) { %>
        <a href="/users/profile" class="nav-btn">Profile</a>
        <a href="/users/logout" class="nav-btn">Logout</a>
      <% } else { %>
        <a href="/users/register" class="nav-btn">Register</a>
        <a href="/users/login" class="nav-btn">Log In</a>
      <% } %>
      <a href="/help" class="nav-btn">Help</a>
    </div>
  </header>

  <div class="auth-container">
    <div class="auth-box">
      <a href="/">
        <img src="/images/PlanIt.png" alt="PlanIt logo" class="logo" />
      </a>
      <p class="subtitle">Welcome back</p>

      <% if (typeof error !== "undefined") { %>
        <p class="error"><%= error %></p>
      <% } %>

      <form method="POST" action="/users/loggedin" class="register-form">
        <input type="text" name="username" placeholder="Username" required />
        <input type="password" name="password" placeholder="Password" required />
        <button type="submit" class="primary-btn">Login</button>
      </form>

      <p class="alt-link">Haven't signed up yet? <a href="/users/register">Sign Up</a></p>
    </div>
  </div>
</body>
</html>