<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Event | PlanIt</title>
  <link rel="stylesheet" href="/main.css">
  <link rel="stylesheet" href="/user.css">
</head>
<body>
  <div class="auth-container">
    <div class="auth-box">
      <a href="/"><img src="/images/PlanIt.png" alt="PlanIt logo" class="logo"></a>
      <p class="subtitle">Edit Event</p>

      <form action="/edit-event/<%= event.event_id %>" method="POST" class="register-form">
        <label for="event_name" class="form-label">Event Name</label>
        <input type="text" id="event_name" name="event_name" value="<%= event.event_name %>" required>

        <label for="event_date" class="form-label">Event Date &amp; Time</label>
        <input type="datetime-local" id="event_date" name="event_date"
               value="<%= event.event_date.toISOString().slice(0,16) %>" required>

        <label for="venue" class="form-label">Venue Name</label>
        <input type="text" id="venue" name="venue" value="<%= event.venue %>" placeholder="e.g., Grand Hall" required>

        <label for="address" class="form-label">Address</label>
        <input type="text" id="address" name="address" value="<%= event.address %>" placeholder="Street, City, ZIP" required>

        <label for="maxGuests" class="form-label">Guest Limit</label>
        <input type="number" id="maxGuests" name="maxGuests" min="1" value="<%= event.maxGuests %>" required>

        <label for="total_budget" class="form-label">Total Budget (£)</label>
        <input type="number" step="0.01" id="total_budget" name="total_budget" value="<%= event.total_budget || '' %>">

        <div class="actions" style="margin-top:20px;">
          <button type="submit" class="primary-btn">Save Changes</button>
          <a href="/landing" class="secondary-btn">Cancel</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
